name: Deploy Backend

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
  workflow_dispatch:

jobs:
  deploy:
    if: github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    environment: demo
    env:
      RENDER_BACKEND_DEPLOY_HOOK_URL: ${{ secrets.RENDER_BACKEND_DEPLOY_HOOK_URL }}
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_BACKEND_SERVICE_ID: ${{ vars.RENDER_BACKEND_SERVICE_ID }}
      RENDER_API_BASE_URL: https://api.render.com/v1

    steps:
      - name: Validate backend service configuration
        run: |
          set -eu
          case "$RENDER_BACKEND_SERVICE_ID" in
            http://*|https://*)
              echo "RENDER_BACKEND_SERVICE_ID must be the Render service ID (for example srv-...), not the public URL."
              exit 1
              ;;
          esac

      - name: Capture current backend deploy id
        id: baseline
        run: |
          set -eu
          response="$(curl --silent --show-error --fail \
            --header "Authorization: Bearer $RENDER_API_KEY" \
            --header "Accept: application/json" \
            "$RENDER_API_BASE_URL/services/$RENDER_BACKEND_SERVICE_ID/deploys")"
          deploy_id="$(printf '%s' "$response" | jq -r '
            def deploy_list:
              if type == "array" then .
              elif type == "object" then
                (.deploys // .items // .data // .result // .deployments // .service.deploys // .service.items // [])
              else [] end;

            deploy_list
            | map(select(type == "object"))
            | map(if has("deploy") and (.deploy | type == "object") then .deploy else . end)
            | .[0].id // .[0].deployId // empty
          ')"
          printf 'baseline_deploy_id=%s\n' "$deploy_id" >> "$GITHUB_OUTPUT"

      - name: Trigger Render backend deploy
        run: |
          set -eu
          if [ -z "${RENDER_BACKEND_DEPLOY_HOOK_URL:-}" ]; then
            echo "RENDER_BACKEND_DEPLOY_HOOK_URL is not set."
            exit 1
          fi

          curl --fail --silent --show-error --request POST "$RENDER_BACKEND_DEPLOY_HOOK_URL" >/dev/null
          echo "Triggered Render backend deploy"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          fetch-depth: 2

      - name: Wait for backend to become ready
        run: sh ./scripts/render-deploy-wait.sh
        env:
          DEPLOY_CHECK: backend
          BASELINE_DEPLOY_ID: ${{ steps.baseline.outputs.baseline_deploy_id }}
